import LoginForm from '@/components/auth/LoginForm';

export const metadata = {
  title: 'Sign In – PayLibre',
  description: 'Log in to your PayLibre account.',
};

export default function LoginPage() {
  return <LoginForm />;
}
