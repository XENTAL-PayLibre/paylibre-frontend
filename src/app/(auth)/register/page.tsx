import RegisterForm from '@/components/auth/RegisterForm';

export const metadata = {
  title: 'Register School – PayLibre',
  description: 'Register your school on the PayLibre network.',
};

export default function RegisterPage() {
  return <RegisterForm />;
}
