export default function OldWay() {
  return (
    <section className="py-20 bg-white">
      <div className="max-w-2xl mx-auto px-4 sm:px-6 text-center">
        <span className="text-xs font-semibold tracking-wide uppercase mb-4 block" style={{ color: 'var(--pl-blue)' }}>
          The old way
        </span>
        <h2 className="text-3xl sm:text-4xl font-bold mb-4" style={{ color: 'var(--pl-text)' }}>
          Reconciliation shouldn&apos;t be a full-time job.
        </h2>
        <p className="text-base leading-relaxed" style={{ color: 'var(--pl-text-secondary)' }}>
          Screenshots in a WhatsApp group. Bank alerts nobody can match to a name. A bursar spending three days a term chasing &quot;who paid what.&quot; It&apos;s not your school&apos;s fault. Paylibre fixes it at the source.
        </p>
      </div>
    </section>
  );
}
